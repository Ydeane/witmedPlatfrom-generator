server:
  port: 80
spring:
  application:
    name: witmedPlatform-generator
  main:
    allow-bean-definition-overriding: true
  thymeleaf:
    cache: false
    encoding: UTF-8
    prefix: classpath:/view/
    suffix: .html